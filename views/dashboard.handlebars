{{> nav/nav-block}}

<div id="main-container" class="container">
  <div class="jumbotron text-center header-wrap" align="center">
    <p class="lead">Welcome to <strong>Follow the Reader</strong></p>
    <hr class="my-4">
    <div>
       <img src="https://source.unsplash.com/hmCMUZKLxa4/540x1139">
      {{#if isloggedin}}
        You are logged in as {{user.firstName}}
        <a href="/logout"> <button type="btn" class="btn">Logout</button></a>
      {{else}}
        <p>Don't forget to add books to your profile!</p>
      {{/if}}
      <h5 id="login-err-msg"></h5>
    </div>
   
  </div>
</div>

{{> modal/login-block}}
